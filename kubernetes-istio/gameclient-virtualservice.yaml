apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  namespace: game
  name: gameclient
spec:
  hosts:
  - "*"
  gateways:
  - game-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: gameclient
      weight: 100

